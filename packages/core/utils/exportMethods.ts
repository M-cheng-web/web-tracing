/**
 * 设置额外的 customerId
 * @param {*} id 需要设置的id
 */
export function setCustomerId(id) {
  base.customerId = id;
}

/**
 * 设置额外的 userUuid
 * @param {*} id 需要设置的id
 */
export function setUserUuid(id) {
  base.userUuid = id;
}